<div class="modal inmodal fade sm" id="addPlanModal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">×</span>
                    <span class="sr-only">Close</span>
                </button>
                <h4 class="modal-title">Add new meal plan</h4>
            </div>

            <form action="{{ path('meal_create_plan', { 'plan': plan.id }) }}" method="POST" autocomplete="off">
                <input type="hidden" name="id">
                <input type="hidden" name="parent_id">
                <div class="modal-body">
                    <div class="form-group">
                        <label for="mealTitle" class="control-label">Title of meal plan</label>
                        <input type="text" id="mealTitle" name="name" class="form-control"
                               placeholder="Title of meal plan">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-block btn-success btn-upper">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>